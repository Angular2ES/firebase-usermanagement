import { Component } from '@angular/core';

@Component({
  selector: 'app-test-login',
  template: `<ng-login-register-template></ng-login-register-template>`
})

export class LoginTestComponent {
} 
